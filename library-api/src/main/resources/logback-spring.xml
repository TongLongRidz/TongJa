<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console Appender: This sends log messages to the console -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Root logger configuration: All logs will go through this by default -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

    <logger name="org.springframework.data.web.config.SpringDataJacksonConfiguration" level="OFF"/>
</configuration>
